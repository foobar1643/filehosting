{% extends "skeleton.twig" %}
{% block pageTitle %}{% trans %} Application settings {% endtrans %}{% endblock %}
{% set navLink = 'settings' %}
{% block pageBody %}
<div class="container-fluid">
    <div class="row" id="comments-section">
        <div class="col-md-12">
            <div class="page-header">
                <h3>{% trans %} Application settings {% endtrans %} </h3>
            </div>
            <div class="panel panel-default">
                <div class="panel-body">
                    <form method="post">
                    <div class="form-group">
                        <label for="language">{% trans %} Language {% endtrans %}</label>
                        <select class="language-select" name="language">
                            {% for language in locales.getAvailableLanguages %}
                                <option value="{{ language }}" {% if language == lang %}selected{% endif %}>{{ locales.getLanguageDisplayName(language) }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-default">{% trans %} Save {% endtrans %}</button>
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}